# Yarn v1: permite instalar/rodar scripts com Node local (ex.: 23) sem falhar no campo engines.
# O build oficial continua na imagem Docker (Node 22). `yarn build` local em Node 23 pode falhar no Next — use Docker ou `nvm use`.
ignore-engines true
